{
  "name": "paper-plane-run",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "verify:bundle-budget": "node scripts/bundle-budget.mjs",
    "build:ios": "BASE_PATH=./ VITE_API_BASE=https://paper-plane-run.vercel.app vite build --outDir ios-dist --emptyOutDir && node scripts/postprocess-ios.mjs && node scripts/sync-ios-web.mjs",
    "verify:ios-parity": "node scripts/verify-ios-parity.mjs",
    "ios:generate": "npm run build:ios && cd ios && xcodegen generate",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^8.1.1",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
